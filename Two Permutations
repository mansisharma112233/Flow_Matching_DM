import sys

data = sys.stdin.read().split()
idx = 0

t = int(data[idx])
idx += 1

for _ in range(t):
    n = int(data[idx]); a = int(data[idx+1]); b = int(data[idx+2])
    idx += 3

    if a + b + 2 <= n or (a == b == n):
        print("Yes")
    else:
        print("No")
